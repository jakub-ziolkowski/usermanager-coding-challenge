server:
  port: 8080
  error:
    include-message: always
logging:
  level:
    root: info
jdbc:
  driverClassName: org.h2.Driver
  url: jdbc:h2:mem:usermanager;DB_CLOSE_DELAY=-1
hibernate:
  dialect: org.hibernate.dialect.H2Dialect
  hbm2ddl:
    auto: create